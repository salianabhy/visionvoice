services:
  - type: web
    name: visionvoice-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
```

**`backend/Procfile`** (Render also accepts this format)
```
web: python app.py